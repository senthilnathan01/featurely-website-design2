{
  "name": "featurely-ai-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check main.js && node --test scripts/site.test.mjs",
    "test": "npm run check"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
